# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

**PSPSS — *Probably Significant Statistical Software*** is a satirical puzzle game about
p-hacking and the replication crisis. Each "study" is a dataset rigged so the honest analysis
is not a win; the player tortures it to significance (or a high Bayes factor) in the fewest
moves by applying real Questionable Research Practices. The UI parodies SPSS/JASP.

The shipped artifact is a single self-contained **`index.html`**, generated by `node build.js`
from the modular sources in `src/`. Edit `src/`, never `index.html`.

Six campaigns: **C1 Publish or Perish** (classic data-torture QRPs), **C2 The Methods
Section** (abuse the *analysis*), **C3 In Bayes We Trust** (metric = **Bayes factor**), **C4 Open
Science** (the redemption arc — the win flips to a *defensible conclusion* via preregistration,
power, multiple-comparison correction, equivalence/TOST, honest multiverse, replication; the QRP
arsenal is present but loses because it raises suspicion), **C5 Mixed Signals** (a *(generalized)
mixed-model masterclass* — re-teaches pseudoreplication/random-slopes harder, then naive df,
within/between conflation, wrong clustering level, clustered-binary pooling, ignored
overdispersion, and a forking-paths capstone; uses a real **GLMM** engine), **C6 Correlation Street**
(*causal-inference QRPs* — post- and pre-treatment colliders/M-bias, the Table 2 fallacy,
weak/invalid instruments via real **2SLS**, adjustment-set shopping, and a DAG capstone; every effect
is a manufactured artefact). Plus free-play **Sandbox Lab** and **Meta-Science Lab** (p-curve /
funnel / power / equivalence).

## Commands

```sh
node build.js              # bundle src/* -> index.html
./test.sh                  # rebuild + run the full suite

node src/stats.test.js     # stats engine vs hand-derived / known references
node src/lmm.test.js       # linear mixed model vs the cluster-means equivalence
node src/glmm.test.js      # GLMM (Laplace) vs lme4 `cbpp` anchor + exact log-OR/log-RR + OLRE SE
node src/bayes.test.js     # Bayes factors: dual-integration + BayesFactor `sleep` anchor
node src/knowledge.test.js # Codex/debrief/quiz/achievement data is complete & consistent
node src/levels.verify.js  # every level: non-winning raw, solvable at par, decoys don't win; truth metadata present
node src/ui.smoke.js       # drives the real ui.js through a DOM shim (Node/require path)
node src/bundle.smoke.js   # runs the built index.html the browser way (global path); build first
node src/tune-seeds.js     # C1 seed search; only after changing a generator
node src/tune-seeds.c2.js  # C2 seed search (LMM levels use a smaller range)
node src/tune-seeds.c3.js  # C3 (Bayesian) seed search
node src/tune-seeds.c5.js  # C5 ((G)LMM) seed search; small ranges (each fit is a full optimisation)
node src/tune-seeds.c6.js  # C6 (causal) seed search (OLS/2SLS — fast, full ranges)
```

Bare `node` only — no npm/package.json. No browser/jsdom in CI, so UI changes are validated via
the two headless smoke harnesses. (A real Firefox + `:1` display exists for manual screenshots:
`firefox --screenshot out.png --window-size=1200,1000 file://$PWD/index.html`.)

## Architecture

Modules load (and bundle) in `build.js`'s `ORDER`; each is a UMD-style IIFE working in **both**
Node (`module.exports`) and the browser (global assignment). Preserve that dual nature — it's
what lets the game logic be unit-tested headlessly. Math libs load before `engine.js`;
`charts.js` before `ui.js`.

- **`stats.js`** (`Stats`) — real stats: incomplete-beta/erf, normal & t CDFs, descriptives,
  Student/Welch t, paired t, Mann-Whitney, Wilcoxon, Pearson, OLS, ANCOVA, `tsls` (two-stage least
  squares / IV, C6); effect size + CIs; and the C4 antidotes `probit`, `requiredN` (a-priori power),
  `adjustP` (Bonferroni/BH), `tost` (equivalence).
- **`lmm.js`** (`PSPSS_lmm`) — genuine REML linear mixed model (random intercept + slope) via
  Nelder-Mead, GLS, between-within df. (C2/C5.)
- **`glmm.js`** (`PSPSS_glmm`) — genuine Laplace-approximation GLMM: binomial(logit)/Poisson(log),
  scalar random intercept(s) + optional observation-level RE (overdispersion), PIRLS inner loop +
  Nelder-Mead/golden-section over the variance components, Wald-z inference. Reuses lmm.js's
  Cholesky/Nelder-Mead. Validated vs the lme4 `cbpp` glmer anchor. (C5.)
- **`bayes.js`** (`PSPSS_bayes`) — JZS Bayes factors for t-tests (Rouder 2009): 1-D integral
  over the prior scale, one-sided BF, BF₀₁. (C3.)
- **`rng.js`** — seeded mulberry32 + Box-Muller (`.next()` = uniform[0,1), `.normal()`, `.int()`).
- **`levels.js`** / **`levels.c2.js`** … **`levels.c6.js`** (`PSPSS_levels`) —
  `build(seed)` returns deterministic data + a pinned tuned `seed`; C2–C6 levels carry
  `evaluate(state, ctx)` (ctx has `Stats`/`LMM`/`GLMM`/`Bayes`). C4 levels set `objective:'honest'`
  and return `decisionWin` (the goal is a defensible conclusion, not crossing the metric). C5 reuses
  the C2 torture-to-significance model with new tools (`choose-df`, `fit-glmm`) + the GLMM engine; C6
  adds causal tools (`report-coefficient`, `use-instrument`) + 2SLS.
- **`campaigns.js`** (`PSPSS_campaigns`) — campaign grouping + gated unlock.
- **`engine.js`** (`PSPSS_engine`) — state, the QRP `TOOLS` registry, paradigm-agnostic
  `analyze` (→ `finalize`), `applyTool`, `toolEnabled`, scoring, win/lose. Dispatches to a level's
  `evaluate` when present.
- **`charts.js`** (`PSPSS_charts`) — SVG figure renderers; diagnostics return `{fn, args}`.
- **`knowledge.js`** (`PSPSS_knowledge`) — the educational layer: `QRP_INFO` (per-flaw Codex/debrief
  entries with citations + antidotes + `verdict`), `TOOL_LABEL`, `QUIZ_ITEMS`/`QUIZ_OPTIONS`,
  `ACHIEVEMENTS` + `evaluateAchievements`. Pure data/logic, consumed by `ui.js`.
- **`content.js`** (`PSPSS_content`) — flavor text keyed by mode.
- **`ui.js`** — the SPSS-parody DOM + campaign picker, plus the post-level **Debrief** (truth reveal),
  **Methods Codex**, **Career Dashboard** (+achievements/career stats in localStorage), **Sandbox
  Lab**, **Spot-the-QRP quiz**, effect-size/CI output, and the **HOUSE RULE** banner. Browser only.
  Campaigns gate in order; a **Reviewer 2 backdoor** (start-screen link, password in `UNLOCK_WORDS`)
  sets `App.unlocked` (localStorage `pspss_unlock`) to bypass gating without faking progress/stars.
  **Gauntlet mode** (`App.gauntlet`, briefing button): replays a level on a *fresh random seed*
  instead of the pinned tuned one — the QRP is not guaranteed to crack it (the "fish doesn't always
  bite" replay/hard mode). Practice only: `recordResult` is skipped so it never touches the career.
  This does NOT relax the no-hints invariant — it adds variance, not assistance.

### Key invariants

- **Stats are real, never faked.** Verify any change against the relevant `*.test.js`. The GLMM
  (`glmm.js`) is anchored to the published lme4 `cbpp` glmer fit + exact saturated-model log-OR/log-RR;
  any change must keep `glmm.test.js` green.
- **Educational layer.** Every level carries `truth: {exists, higher?}` (the data-generating reality)
  so the Debrief can reveal real-effect-obtained-invalidly vs. manufactured false positive. Every
  level `flaw` must have a `QRP_INFO` entry; every intervention tool a `TOOL_LABEL` (asserted by
  `knowledge.test.js`). `analyze()` returns `effect` (Cohen's d) + `ci` (95%); for custom evaluators
  pass `aArr`/`bArr` (group arrays) and `finalize` derives them. Difficulty is uniform — no
  tutorial/hints/easy-mode by design ("being a good researcher ≠ being good at p-hacking").
- **Every level is a real puzzle.** `levels.verify.js` proves: raw is not a win; the intended QRP
  wins at `par`; from C2 on every offered tool applies without error and the intended option is among
  the winners (a c2 trap has none). Re-run after any level/engine change; retune with `tune-seeds*.js`.
- **Campaigns 2, 3, 5 & 6 expose the FULL arsenal** (the fix for "levels were just clicking"). Loops at
  the end of `levels.c2.js`/`levels.c3.js`/`levels.c5.js`/`levels.c6.js` set every level's
  `allowedTools = ALL_QRP` (all interventions) and add generic decoy data (`lmm`, `moderator`,
  `aggregable`, `tests`, `candidateControls`, `specs`; C5 also `dfTestable`, `glmm`; C6 also
  `coefficients`, `instruments`) so the flag-based tools all appear. Each level's `evaluate` honours
  only its own flag, so most tools are inert — but the player must still *find* the one(s) that work.
  Data-mutators (refine/winsorize/log) genuinely mutate the data, so a few levels have realistic
  alternate p-hacks; that's allowed (verify only requires the intended to win, not uniqueness). The C5
  capstone (`forking-models`) and C6 capstone (`the-dag`) are genuine multi-move chains — no single
  move wins. Do NOT shrink menus back to per-level whitelists. (C1 stays curated as a gentle intro.)
  Menus show only *enabled* tools (`ui.js items()`), no greyed clutter.
- **Win metric is paradigm-agnostic.** `analyze()` returns `metricKind` (`'p'`|`'bf'`),
  `metricLabel`, `metricValue`, `goalText`, `significant`, `win`. UI reads those — never hardcode
  "p-value". `win` = metric crosses `level.winThreshold` (default .05 / BF 3) AND direction matches
  `predictedHigher` — UNLESS the level (C4) returns `decisionWin`, which `finalize` then uses verbatim.
- **Campaign 4 inverts the win.** `objective:'honest'` levels judge by `decisionWin` (the honest
  pipeline + `suspicion === 0`), not the metric. Honest tools (preregister, power-analysis,
  collect-to-power, correct-comparisons, equivalence-test, report-multiverse) cost a move but **0
  suspicion**; the QRP arsenal is present and loses (it raises suspicion). `levels.verify.js` checks
  only honest tools can win C4.
- **Accessibility (don't regress).** Status is colour-independent (✓/✗ text token on the metric card
  + `out-p` glyph). Modals are `role="dialog"` and close on Esc / confirm on Enter; menu labels/items
  are focusable. SVG figures get `role="img"`+`<title>`. Layout is responsive (`@media max-width:1000px`).
  Optional sound (off by default, `pspss_sound`) is guarded for headless/Node.
- **The DOM shim has limits.** It does not parse `innerHTML` strings into queryable nodes (build
  real nodes if you need to query them) and needs `createElementNS` for SVG. Both smoke harnesses
  must pass.

> Recovery note: the source can be reconstructed from any bundled `index.html` — modules are
> concatenated with `// ===================== <file> =====================` headers and the CSS sits
> in the `<style>` block. There is no git history; consider committing.
