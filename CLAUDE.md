# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

**PSPSS — *Probably Significant Statistical Software*** is a satirical puzzle game about
p-hacking and the replication crisis. Each "study" is a dataset rigged so the honest analysis
is not a win; the player tortures it to significance (or a high Bayes factor) in the fewest
moves by applying real Questionable Research Practices. The UI parodies SPSS/JASP.

The shipped artifact is a single self-contained **`index.html`**, generated by `node build.js`
from the modular sources in `src/`. Edit `src/`, never `index.html`.

Three campaigns: **C1 Publish or Perish** (classic data-torture QRPs), **C2 The Methods
Section** (abuse the *analysis*: wrong tests, pseudoreplication, mixed-model mis-specification,
median splits, collider control, Simpson's paradox, specification search), **C3 In Bayes We
Trust** (the metric becomes the **Bayes factor**, abused via priors, one-sided priors, optional
stopping, BF₀₁ relabelling).

## Commands

```sh
node build.js              # bundle src/* -> index.html
./test.sh                  # rebuild + run the full suite

node src/stats.test.js     # stats engine vs hand-derived / known references
node src/lmm.test.js       # linear mixed model vs the cluster-means equivalence
node src/bayes.test.js     # Bayes factors: dual-integration + BayesFactor `sleep` anchor
node src/knowledge.test.js # Codex/debrief/quiz/achievement data is complete & consistent
node src/levels.verify.js  # every level: non-winning raw, solvable at par, decoys don't win; truth metadata present
node src/ui.smoke.js       # drives the real ui.js through a DOM shim (Node/require path)
node src/bundle.smoke.js   # runs the built index.html the browser way (global path); build first
node src/tune-seeds.js     # C1 seed search; only after changing a generator
node src/tune-seeds.c2.js  # C2 seed search (LMM levels use a smaller range)
node src/tune-seeds.c3.js  # C3 (Bayesian) seed search
```

Bare `node` only — no npm/package.json. No browser/jsdom in CI, so UI changes are validated via
the two headless smoke harnesses. (A real Firefox + `:1` display exists for manual screenshots:
`firefox --screenshot out.png --window-size=1200,1000 file://$PWD/index.html`.)

## Architecture

Modules load (and bundle) in `build.js`'s `ORDER`; each is a UMD-style IIFE working in **both**
Node (`module.exports`) and the browser (global assignment). Preserve that dual nature — it's
what lets the game logic be unit-tested headlessly. Math libs load before `engine.js`;
`charts.js` before `ui.js`.

- **`stats.js`** (`Stats`) — real stats: incomplete-beta/erf, normal & t CDFs, descriptives,
  Student/Welch t, paired t, Mann-Whitney, Wilcoxon, Pearson, OLS, ANCOVA. Every frequentist p.
- **`lmm.js`** (`PSPSS_lmm`) — genuine REML linear mixed model (random intercept + slope) via
  Nelder-Mead, GLS, between-within df. (C2.)
- **`bayes.js`** (`PSPSS_bayes`) — JZS Bayes factors for t-tests (Rouder 2009): 1-D integral
  over the prior scale, one-sided BF, BF₀₁. (C3.)
- **`rng.js`** — seeded mulberry32 + Box-Muller.
- **`levels.js`** / **`levels.c2.js`** / **`levels.c3.js`** (`PSPSS_levels`) — `build(seed)`
  returns deterministic data with one exploitable flaw + a pinned tuned `seed`. C2/C3 levels also
  carry `evaluate(state, ctx)` running their own honest analysis via `ctx.Stats`/`ctx.LMM`/`ctx.Bayes`.
- **`campaigns.js`** (`PSPSS_campaigns`) — campaign grouping + gated unlock.
- **`engine.js`** (`PSPSS_engine`) — state, the QRP `TOOLS` registry, paradigm-agnostic
  `analyze` (→ `finalize`), `applyTool`, `toolEnabled`, scoring, win/lose. Dispatches to a level's
  `evaluate` when present.
- **`charts.js`** (`PSPSS_charts`) — SVG figure renderers; diagnostics return `{fn, args}`.
- **`knowledge.js`** (`PSPSS_knowledge`) — the educational layer: `QRP_INFO` (per-flaw Codex/debrief
  entries with citations + antidotes + `verdict`), `TOOL_LABEL`, `QUIZ_ITEMS`/`QUIZ_OPTIONS`,
  `ACHIEVEMENTS` + `evaluateAchievements`. Pure data/logic, consumed by `ui.js`.
- **`content.js`** (`PSPSS_content`) — flavor text keyed by mode.
- **`ui.js`** — the SPSS-parody DOM + campaign picker, plus the post-level **Debrief** (truth reveal),
  **Methods Codex**, **Career Dashboard** (+achievements/career stats in localStorage), **Sandbox
  Lab**, **Spot-the-QRP quiz**, effect-size/CI output, and the **HOUSE RULE** banner. Browser only.
  Campaigns gate in order; a **Reviewer 2 backdoor** (start-screen link, password in `UNLOCK_WORDS`)
  sets `App.unlocked` (localStorage `pspss_unlock`) to bypass gating without faking progress/stars.

### Key invariants

- **Stats are real, never faked.** Verify any change against the relevant `*.test.js`.
- **Educational layer.** Every level carries `truth: {exists, higher?}` (the data-generating reality)
  so the Debrief can reveal real-effect-obtained-invalidly vs. manufactured false positive. Every
  level `flaw` must have a `QRP_INFO` entry; every intervention tool a `TOOL_LABEL` (asserted by
  `knowledge.test.js`). `analyze()` returns `effect` (Cohen's d) + `ci` (95%); for custom evaluators
  pass `aArr`/`bArr` (group arrays) and `finalize` derives them. Difficulty is uniform — no
  tutorial/hints/easy-mode by design ("being a good researcher ≠ being good at p-hacking").
- **Every level is a real puzzle.** `levels.verify.js` proves: raw is not a win; the intended QRP
  wins at `par`; from C2 on every offered tool applies without error and the intended option is among
  the winners (a c2 trap has none). Re-run after any level/engine change; retune with `tune-seeds*.js`.
- **Campaigns 2 & 3 expose the FULL arsenal** (the fix for "levels were just clicking"). Loops at the
  end of `levels.c2.js`/`levels.c3.js` set every level's `allowedTools = ALL_QRP` (all interventions)
  and add generic decoy data (`lmm`, `moderator`, `aggregable`, `tests`, `candidateControls`,
  `specs`) so the flag-based tools all appear. Each level's `evaluate` honours only its own flag, so
  most tools are inert — but the player must still *find* the one(s) that work. Data-mutators
  (refine/winsorize/log) genuinely mutate the data, so a few levels have realistic alternate p-hacks;
  that's allowed (verify only requires the intended to win, not uniqueness). Do NOT shrink menus back
  to per-level whitelists. (C1 stays curated as a gentle intro.) Menus show only *enabled* tools
  (`ui.js items()`), no greyed clutter.
- **Win metric is paradigm-agnostic.** `analyze()` returns `metricKind` (`'p'`|`'bf'`),
  `metricLabel`, `metricValue`, `goalText`, `significant`, `win`. UI reads those — never hardcode
  "p-value". `win` = metric crosses `level.winThreshold` (default .05 / BF 3) AND direction matches
  `predictedHigher`.
- **The DOM shim has limits.** It does not parse `innerHTML` strings into queryable nodes (build
  real nodes if you need to query them) and needs `createElementNS` for SVG. Both smoke harnesses
  must pass.

> Recovery note: the source can be reconstructed from any bundled `index.html` — modules are
> concatenated with `// ===================== <file> =====================` headers and the CSS sits
> in the `<style>` block. There is no git history; consider committing.
